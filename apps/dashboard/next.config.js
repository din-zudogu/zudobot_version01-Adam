/** @type {import('next').NextConfig} */
const nextConfig = {
  // NEXT_PUBLIC_API_URL — override in .env.local for production
  // Default: http://localhost:4000 (local api dev server)
};

module.exports = nextConfig;
