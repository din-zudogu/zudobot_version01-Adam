{
  "name": "zudobot",
  "version": "0.1.0",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev:cover": "npm run dev --workspace=apps/cover",
    "build:cover": "npm run build --workspace=apps/cover",
    "start:cover": "npm run start --workspace=apps/cover",
    "build:widget": "npm run build --workspace=packages/widget"
  },
  "dependencies": {
    "@aws-sdk/client-amplify": "^3.750.0"
  }
}
