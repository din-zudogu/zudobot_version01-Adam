# IAM credentials for npm run dev:amplify / db:amplify-env (copy to .env.aws.local)
# Do NOT commit .env.aws.local — used only to call Amplify API, not app secrets.

ACCESS_KEY_ID=AKIAxxxxxxxxxxxxxxxx
SECRET_ACCESS_KEY=your-secret-access-key
AWS_REGION=ap-southeast-2
